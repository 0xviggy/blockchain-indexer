reach this repo struct:

root -> readme
root/docs -> tech_spec, business_spec, design_decisions, progress_tracking
No need for a readme in docs.
docs/setup/* all files seems extra. should consolidate into a setup file in docs.
All of tech_spec, business_spec, design_decisions, progress_tracking, docs/setup/* seem to also have redundant items. do a deep dive a cleanup.

Also, all the db specific items can go in the readme within database folder

Finally have a proper reference in root readme to all files in docs and learning

